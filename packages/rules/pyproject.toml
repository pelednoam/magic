[project]
name = "mtgcoach-rules"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["mtgcoach-coach"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mtgcoach"]

[tool.uv.sources]
mtgcoach-coach = { workspace = true }
